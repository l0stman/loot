This is a toy scheme interpreter implemented in C.  To compile it:
> make depend && make
And to install
> make install clean
This will install the binary under $HOME/bin so it should be on your
$PATH environment  variable.
